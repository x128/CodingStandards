# .review/land.conf — landing and advancing switches.
#
# Belongs to the project, not the CodingStandards submodule.
#
# Read as DATA, never sourced — `KEY=value` per line, one value each. Shell
# expansions, command substitutions and functions here do nothing: the scripts
# with commit authority must not be redefinable from a file the project edits.
# Inline `# comments` after a value are stripped.
#
# Two kinds of key live here, and the difference matters:
#
#   Enforced by a script — LANDING, THRESHOLD, PROTECTED_BRANCHES. `land.sh` and
#   `run.sh` read these. Setting them changes what the pipeline can do, whatever
#   any agent believes.
#
#   Read by the maintainer — ADVANCE, ADVANCE_CATEGORIES, RISK_PATHS,
#   CONFIRM_AFTER. No line of shell reads these; they are instructions to a
#   model, followed as reliably as a model follows instructions. They govern
#   whether you get asked, never whether a commit is allowed — everything that
#   gates a commit is in the first group, by design.

# ---------------------------------------------------------------------------
# Landing — may the maintainer commit?
# ---------------------------------------------------------------------------

# on  — land.sh commits when all nine conditions hold
# off — land.sh checks everything, then prints the commit for you to run
# Start at off. Turn it on once you have watched the refusals behave.
LANDING=off

# Findings at or above this severity block landing.
THRESHOLD=major

# Additional protected branches. `main` and `master` are built into land.sh and
# cannot be removed here — this list only ever adds to them.
PROTECTED_BRANCHES=""

# ---------------------------------------------------------------------------
# Advancing — may the maintainer take the next task without asking?
# ---------------------------------------------------------------------------

# ask  — every finished task is shown to you before the next one starts
# auto — categories listed in ADVANCE_CATEGORIES advance silently; the rest ask
# Start at ask, and stay there until the shadow metric earns a category
# (MAINTAINER_TRAINING.md § Track B).
ADVANCE=ask

# Space-separated task categories that have earned silence. Unlock one at a
# time, on ~20 clean samples. One objection reverts a category to ask.
ADVANCE_CATEGORIES=""

# Paths that always ask, whatever the log says. Some code never earns silence.
RISK_PATHS="src/auth/* src/payments/* src/db/migrations/*"

# How many later tasks in the same area must land before an accepted task counts
# as confirmed rather than provisional. Your approval at the boundary is quick;
# the consequences surface tasks later. Only confirmed samples unlock a category.
CONFIRM_AFTER=5
